# v0.8.5 · 文明 VI 陪练

一个原生 macOS 中文陪练：只读获取《文明 VI》的当前局势，给出简短行动建议，并支持聊天追问。无需逐张截图，不替玩家操作游戏。

## 本次发布

- 主窗口、设置和普通快捷小窗同步预览强调色与深浅外观，无需先保存；取消恢复原设置。
- 多套 OpenAI 兼容 API 配置，支持新增、修改、删除和切换，密钥保存在本机钥匙串。
- 菜单栏「文」优先打开不透明卡片；透明模式支持固定、移动、边框缩放、首次引导及右键文字样式设置。
- 历史对话浏览、内置学习目标与自定义目标。
- 快速建议与聊天分别设置输出预算，避免无节制的长回复；达到限制时保留已有内容并标记未完成。

## 安装

下载附件 `文明VI陪练-0.8.5-macOS.zip`，解压并将应用移至「应用程序」。需要 macOS 13+ 和 Apple Silicon；Intel 尚未验证，可尝试源码构建。

游戏连接需手动启用 `EnableTuner 1`、重启游戏并进入单人地图。启用 Tuner 会影响成就。详细步骤和 API 配置见 [README](https://github.com/mxymalay/civ6-coach/blob/v0.8.5/README.md)。

## 注意事项

- 应用为 ad-hoc 签名，未经过 Apple 公证。若被系统拦截，核实来源后按 macOS 的隐私与安全提示操作，不要关闭系统安全保护。
- 不读取迷雾，不自动操作游戏；只有请求建议、聊天或测试连接时才调用配置的 AI 服务，可能产生费用。
- 单人 Steam Mac 版为主要验证环境；DLC、Mod 和游戏全屏模式可能影响数据或叠加效果。
- 测试共 52 项：50 项通过、2 项可选真实 API／实局测试跳过；Release 构建和签名验证通过。近期跨窗口视觉交互尚未全面人工验收。
- 非官方项目，与 Firaxis、2K 或 OpenAI 无隶属关系。
