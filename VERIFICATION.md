# 更新 · 0.8.4 历史对话

- 聊天页新增历史入口，按消息时间倒序读取本机 conversation-*.json 归档；左侧列表、右侧只读消息，保持当前对话和 AI 上下文不变。
- 排除当前对话、空归档、目录和符号链接；损坏归档不删除，提示无法读取的数量。
- 新增读取排序、当前文件不变、损坏文件保留和目录不存在测试；51 项测试中 49 项通过、2 项可选测试跳过。Release 构建及签名检查通过，尚未做原生窗口视觉验收。

# 更新 · 0.8.3 菜单栏优先普通卡片

- 菜单栏打开快捷小窗时恢复不透明卡片；若透明层正在显示，点击菜单栏改为普通卡片，不先隐藏。普通卡片仍可再次点击收起。
- 保留两种模式各自尺寸、固定状态和透明文字偏好；手动重新引导仍能直接显示透明层。
- 49 项测试中 47 项通过、2 项可选测试跳过；Release 构建及签名检查通过，未进行原生窗口视觉验收。
- 排查确认聊天界面缺少归档入口，当前实现仅加载 conversation.json；本次未修改用户聊天文件或归档内容。

# 更新 · 0.8.2 透明小窗引导与文字样式

- 首次进入透明模式显示滚动区内的可关闭引导，包含拖动、缩放、右键和退出；关闭后记住已读状态。右键和设置都能手动重新打开，设置可关闭自动引导。
- 右键文字设置提供七档字号、四种字体设计、五种颜色和四档阴影；设置新增透明小窗页，偏好即时生效并单独保存。
- 四角添加短段深浅双描边，不占用额外点击区域，原缩放热区不变。
- `swift test`：48 项中 46 项通过、2 项可选测试跳过、0 失败；Release 构建及签名验证通过。原生窗口视觉和右键操作尚未人工验收。

# 更新 · 0.8.1 配置入口与小窗外观

- 配置选择器移至聊天输入区左下角原模型名位置，显示配置名与模型名，移除主窗口顶部入口。
- 透明小窗使用内容级 colorScheme，不再向窗口提交深色 preferredColorScheme；普通卡片也不再提交会反向影响系统外观读取的偏好，消除外观反馈路径。
- 46 项测试中 44 项通过、2 项可选测试跳过；本次未进行原生窗口切换的视觉验收。

# 更新 · 0.8.0 多 API 与紧凑控件

- API 配置支持新增、编辑、删除及主窗口直接切换；旧配置保留原钥匙串账户，新配置按唯一 ID 隔离密钥。
- 密钥保存预先读取旧值，失败时回滚；未成功读取的密钥禁止静默覆盖。删除在保存时才生效。
- 学习目标改为五个内置选项与自定义，移除原说明；小窗建议、停止等按钮统一主题样式。
- `swift test`：46 项中 44 项通过，2 项可选真实接口／实局测试跳过，0 失败。Release 构建及签名校验通过；本次未做新版原生窗口视觉验收。

## AI 输出预算与超限处理

- 请求新增服务端输出预算：快速建议 2048 tokens、聊天 4096、连接测试 1024。Responses 使用 max_output_tokens；Chat Completions 对已知 OpenAI 推理模型使用 max_completion_tokens，其余兼容模型使用 max_tokens。
- 快速建议要求最多三条、每条一句、总计不超过 180 字。另设独立正文上限：建议 600 字符、聊天 6000、连接测试 128；达到上限及时关闭请求并保留已接收正文。
- 模型达到 token 预算时保留末尾片段与已有正文，返回 limited 状态；截断建议不标作完整，截断聊天不进入后续完整历史。未知中断、内容过滤及无正文仍报错。
- 传输数据与正文分开计量：单事件仍限制 2 MB，总流量限制 16 MB；正常推理/元数据超过旧 2 MB 总限制后仍能接收短答案。
- 两项回归先在旧代码复现失败：万字回复未经限制进入界面；推理数据超过 2 MB 导致短答案被拒绝。修复后通过，并覆盖两种协议、流式与 JSON 回退、token 耗尽、无正文和异常传输上限。
- 使用已配置 deepseek-flash 做了一次真实接口验证，仅发送模拟游戏场景：约 6.3 秒返回 99 字，正常完成，未发送真实游戏数据。此验证不等于复现了截图当时的服务端流内容。

# 更新 · 0.7.7 界面文案与聊天布局

- 移除外观页两段冗长说明、设置底部保存说明、设置和聊天页的装饰性副标题；保留费用、密钥和隐私等必要提示。
- 断线时保持内部重连与状态显示，但不再把底层连接错误文本重复铺在主页、快捷卡片和透明层。
- 聊天改为玩家气泡靠右、头像在右，老师气泡靠左、头像在左；限制气泡宽度并区分底色。
- 透明叠加层只显示前两条即时提醒的标题、最多五行 AI 建议；错误缩为一行并保留完整悬停提示。主窗口继续保留完整信息。
- 2 MB API 流安全上限不变；错误文案改为“响应流”超限，避免误以为只是界面文字过长。
- `swift test`：34 项中 33 项通过，1 项实局测试按设计跳过，0 失败；Release 构建、签名和 ZIP 完整性检查通过。

# 更新 · 0.7.6 启动即陪练

- 移除主窗口侧栏、快捷小窗及应用菜单中的陪练开关／暂停入口；应用初始化时自动启动本地游戏局势轮询。
- 游戏未载入时显示等待连接，刷新、聊天与建议不再要求额外开启。AI 仍只在点击建议、测试连接或发送聊天时调用。
- 新增启动状态回归测试：旧版先失败，改动后通过。
- `swift test`：34 项中 33 项通过，1 项实局测试按设计跳过，0 失败；Release 构建、签名及 ZIP 完整性检查通过。

# 更新 · 0.7.5 主窗口图钉与连接状态

- 主窗口置顶从设置开关移至右上角图钉，状态即时生效并沿用原设置持久化字段；快捷卡片图钉保持独立。
- API 测试结果与按钮同排，成功、失败和取消分别有图标与颜色；编辑地址、模型、接口类型或密钥会取消旧测试并清除旧结果。
- 设置页补充说明学习目标是一段附加的 AI 教学指导文字，提供可填写示例，不把它误称为预设模板。
- `swift test`：33 项中 32 项通过，1 项实局测试按设计跳过，0 失败。

# 更新 · 0.7.4 输入控件统一

- 设置页的 API 地址、模型、密钥和学习目标，以及聊天输入区改用主题化背景、边框与聚焦反馈。
- 设置分类、接口类型、外观模式和刷新间隔改用一致的紧凑选项条；设置开关跟随强调色。
- Release 编译与签名校验通过。为避免干扰正在运行的 0.7.3 版本，没有切换到新版窗口做原生视觉验收；本次未运行自动化测试。

# 更新 · 0.7.3 主页精简

- 主页收敛为回合/连接状态、资源摘要、本地回合变化、最多三项基础提醒和建议卡；收起重复欢迎教程、隐私说明及冗长介绍。
- 侧栏缩短品牌、副标题、连接说明，保留陪练开关、对局回合、主导航和设置。
- Release 构建与签名完整性检查通过。本次未运行自动化测试。

# 更新 · 0.7.2 外观选项

- 外观页拆分强调色与外观模式：强调色为森林绿、海军蓝、暮光紫、石墨黑、银灰；外观为跟随系统、浅色、深色。
- 主窗口、普通快捷卡片和设置预览使用相同的独立设置；透明游戏层保留深色高对比文字。
- 旧版白色主题迁移到银灰强调色与浅色外观，旧版其余主题保留强调色并沿用深色显示。
- Release 构建与签名校验通过。本次只做 Release 编译检查，未运行自动化测试。

# 验证补充 · 0.7.1 窗口交互

- 窗口原生事件统一处理顶部拖动和透明层右键菜单。鼠标拖动区避开顶部按钮及四边/四角缩放热区；测试覆盖两种模式的标题栏可拖区与排除区域。
- 透明层右键（含 Control+单击）提供生成/停止以及退出透明。菜单始终使用明确的生成建议文案，状态不允许请求时禁用并说明条件；生成动作复用小窗快捷按钮逻辑，退出只切回普通卡片。
- 最新 `swift test`：33 项中 32 项通过，1 项实局测试跳过，0 失败。
- 隔离桌面验证时测试副本与现有应用窗口被系统工具映射到同一进程，拖动实测无法安全归属该副本；所以代码层与测试已验证，独立窗口的实际拖动和右键菜单显示仍需用户试用确认。

# 验证记录 · 0.7.0

- 普通卡片固定与透明叠加层分离；透明自动保持置顶，退出恢复普通卡片原来的图钉状态。两种模式分别保存尺寸，旧版固定状态迁移到透明模式。
- 普通卡片改为紧凑标题和底部小按钮；透明模式只有文字、淡虚线边框和两个小图标。移除原生标题栏占位与常驻缩放图标。
- 边缘 8 像素、角落 14 像素热区；几何测试覆盖中心不命中、左上角双轴缩放、最小尺寸和屏幕边界。极低透明度边缘事件表面覆盖虚线间隙。
- 最新 `swift test`：32 项中 31 项通过，1 项实局测试跳过，0 失败。覆盖固定不触发透明、模式切换、双尺寸与状态重启持久化。
- Release 构建和分发副本的 ad-hoc 签名完整性校验通过，已生成 0.7 应用与 ZIP。
- 原生界面确认普通卡片可单独固定，透明入口显示独立布局，顶部拖动区不再伸展占位；未调用真实 AI。
- 坐标拖动检查发生焦点切换，未取得可靠的缩放实测证据；四边/角落连续拖动、最小尺寸实机布局、跨游戏全屏叠加仍未完整验收。单窗口 JPEG 截图不含 alpha，不能用它证明实际游戏透出效果。

## 历史记录 · 0.6.0

- 固定模式改为 borderless 非激活浮动面板，设置 `isOpaque=false`、透明窗口背景、无窗口阴影；SwiftUI 内容没有实色底板。取消固定恢复完整主题小窗。
- 叠加层仅保留状态、建议文字与空心操作按钮；顶部悬停控制提供暂停、取消固定和收起。最小尺寸 300×200，普通模式保持 360×420。
- 新增覆盖固定模式尺寸、透明/阴影策略、取消固定恢复尺寸、屏幕边界及持久化，并测试显式缩放手柄的左上角锚定与尺寸限制。
- 用户反馈后将叠加层操作按钮缩小为右下角空心按钮，新增 30×30 常显缩放手柄，普通小窗同步提供。
- 原生界面已检查透明模式精简内容、空心按钮，以及取消固定恢复完整菜单；无需真实 AI 请求。
- 捕获服务输出的是 JPEG 单窗口截图，不含透明通道，不能凭该截图证明游戏画面的透出效果。全屏游戏叠加、悬停与拖动缩放的完整实机验收仍需继续；不宣称全窗鼠标穿透。

## 历史记录 · 0.5.0

- 地图从第一座城市扩展到全部己方城市和单位周围 3 格的当前可见地块；按地块索引去重，采集上限 4000，并输出覆盖范围与截断标记。
- AI 地图细节最多 240 地块、400 单位；本地保留完整已采集数据用于回合比较，AI 上下文标注抽样。
- 回合变化覆盖人口、住房阈值、生产变化、金币净收入转负、新城市、科技解锁和新敌军观察；没有自动 AI 请求。
- Swift 离线测试覆盖首轮、重复轮询、回退、跨回合、关闭地图、科技采集完整性和上下文预算。
- 实局测试尝试失败：游戏连接在读取前断开，随后端口检查未发现 4318 监听。因此本版本扩大地图的 Lua 采集尚未完成实局验证；不把旧版实测结论当新版验证。

## 历史记录 · 0.4.0

- 移除桌面应用的 Codex 登录、CLI 调用实现及对应测试，不修改系统账号。
- `swift test`：20 项中 19 项通过，1 项实局测试按设计跳过，0 失败。
- 新增回归覆盖旧版设置迁移、固定状态持久化、自动收起策略、窗口尺寸校验与持久化。
- 原生界面已确认 API、外观、陪练与隐私三页可切换，纯白布局可读，API 页无 Codex 入口。
- 小窗改用可缩放 NSPanel；固定保持浮动层级，取消固定后点击外部或失去焦点自动收起。菜单栏为「文」，另有 ⌘⇧J 快捷键。
- Release 构建与 ad-hoc 签名检查通过。完整的菜单栏点击、跨应用置顶和鼠标拖动缩放实测尚未完成：验证期间检测到用户并行操作，未继续抢占窗口。

## 历史记录 · 0.3.0

验证环境：Apple Silicon macOS，Swift 6.4，Steam Civilization VI。

- 默认 `swift test`：21 项中 19 项通过，2 项真实服务测试按设计跳过，0 失败。
- 扩展功能首次集成时启用两个真实服务测试：18/18 通过，确认游戏快照及官方 Codex CLI 授权请求可用。
- 后续新增 3 项回归测试，复现并修复了城市名称影响对局识别、暂停提前释放测试占用、非流式 JSON 截断被当作完成的问题。
- Python 桥接器：5/5 通过，涵盖协议及只读工具边界。
- Release 构建和 ad-hoc 签名完整性检查通过。
- 主窗口旧版的 API 连接测试、快速建议和聊天曾通过界面操作验证；新版设置页的 Codex 授权及纯白主题入口已可见。为不打断用户正在进行的登录，最终版本未重新执行完整的菜单栏/五主题视觉回归，不将此项记为通过。

审查后的其他修复：中断建议标记未完成且不进入后续上下文；恢复 API 地址时恢复原密钥输入；重启应用后显示旧聊天但不自动把未匹配的历史发给 AI。

尚未覆盖：Intel、长时间跨存档耐久、所有 DLC/Mod、全新账号完整设备授权（使用用户已有授权做过真实请求）。
