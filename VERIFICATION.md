# 验证记录 · 0.7.0

- 普通卡片固定与透明叠加层分离；透明自动保持置顶，退出恢复普通卡片原来的图钉状态。两种模式分别保存尺寸，旧版固定状态迁移到透明模式。
- 普通卡片改为紧凑标题和底部小按钮；透明模式只有文字、淡虚线边框和两个小图标。移除原生标题栏占位与常驻缩放图标。
- 边缘 8 像素、角落 14 像素热区；几何测试覆盖中心不命中、左上角双轴缩放、最小尺寸和屏幕边界。极低透明度边缘事件表面覆盖虚线间隙。
- 最新 `swift test`：32 项中 31 项通过，1 项实局测试跳过，0 失败。覆盖固定不触发透明、模式切换、双尺寸与状态重启持久化。
- Release 构建和分发副本的 ad-hoc 签名完整性校验通过，已生成 0.7 应用与 ZIP。
- 原生界面确认普通卡片可单独固定，透明入口显示独立布局，顶部拖动区不再伸展占位；未调用真实 AI。
- 坐标拖动检查发生焦点切换，未取得可靠的缩放实测证据；四边/角落连续拖动、最小尺寸实机布局、跨游戏全屏叠加仍未完整验收。单窗口 JPEG 截图不含 alpha，不能用它证明实际游戏透出效果。

## 历史记录 · 0.6.0

- 固定模式改为 borderless 非激活浮动面板，设置 `isOpaque=false`、透明窗口背景、无窗口阴影；SwiftUI 内容没有实色底板。取消固定恢复完整主题小窗。
- 叠加层仅保留状态、建议文字与空心操作按钮；顶部悬停控制提供暂停、取消固定和收起。最小尺寸 300×200，普通模式保持 360×420。
- 新增覆盖固定模式尺寸、透明/阴影策略、取消固定恢复尺寸、屏幕边界及持久化，并测试显式缩放手柄的左上角锚定与尺寸限制。
- 用户反馈后将叠加层操作按钮缩小为右下角空心按钮，新增 30×30 常显缩放手柄，普通小窗同步提供。
- 原生界面已检查透明模式精简内容、空心按钮，以及取消固定恢复完整菜单；无需真实 AI 请求。
- 捕获服务输出的是 JPEG 单窗口截图，不含透明通道，不能凭该截图证明游戏画面的透出效果。全屏游戏叠加、悬停与拖动缩放的完整实机验收仍需继续；不宣称全窗鼠标穿透。

## 历史记录 · 0.5.0

- 地图从第一座城市扩展到全部己方城市和单位周围 3 格的当前可见地块；按地块索引去重，采集上限 4000，并输出覆盖范围与截断标记。
- AI 地图细节最多 240 地块、400 单位；本地保留完整已采集数据用于回合比较，AI 上下文标注抽样。
- 回合变化覆盖人口、住房阈值、生产变化、金币净收入转负、新城市、科技解锁和新敌军观察；没有自动 AI 请求。
- Swift 离线测试覆盖首轮、重复轮询、回退、跨回合、关闭地图、科技采集完整性和上下文预算。
- 实局测试尝试失败：游戏连接在读取前断开，随后端口检查未发现 4318 监听。因此本版本扩大地图的 Lua 采集尚未完成实局验证；不把旧版实测结论当新版验证。

## 历史记录 · 0.4.0

- 移除桌面应用的 Codex 登录、CLI 调用实现及对应测试，不修改系统账号。
- `swift test`：20 项中 19 项通过，1 项实局测试按设计跳过，0 失败。
- 新增回归覆盖旧版设置迁移、固定状态持久化、自动收起策略、窗口尺寸校验与持久化。
- 原生界面已确认 API、外观、陪练与隐私三页可切换，纯白布局可读，API 页无 Codex 入口。
- 小窗改用可缩放 NSPanel；固定保持浮动层级，取消固定后点击外部或失去焦点自动收起。菜单栏为「文」，另有 ⌘⇧J 快捷键。
- Release 构建与 ad-hoc 签名检查通过。完整的菜单栏点击、跨应用置顶和鼠标拖动缩放实测尚未完成：验证期间检测到用户并行操作，未继续抢占窗口。

## 历史记录 · 0.3.0

验证环境：Apple Silicon macOS，Swift 6.4，Steam Civilization VI。

- 默认 `swift test`：21 项中 19 项通过，2 项真实服务测试按设计跳过，0 失败。
- 扩展功能首次集成时启用两个真实服务测试：18/18 通过，确认游戏快照及官方 Codex CLI 授权请求可用。
- 后续新增 3 项回归测试，复现并修复了城市名称影响对局识别、暂停提前释放测试占用、非流式 JSON 截断被当作完成的问题。
- Python 桥接器：5/5 通过，涵盖协议及只读工具边界。
- Release 构建和 ad-hoc 签名完整性检查通过。
- 主窗口旧版的 API 连接测试、快速建议和聊天曾通过界面操作验证；新版设置页的 Codex 授权及纯白主题入口已可见。为不打断用户正在进行的登录，最终版本未重新执行完整的菜单栏/五主题视觉回归，不将此项记为通过。

审查后的其他修复：中断建议标记未完成且不进入后续上下文；恢复 API 地址时恢复原密钥输入；重启应用后显示旧聊天但不自动把未匹配的历史发给 AI。

尚未覆盖：Intel、长时间跨存档耐久、所有 DLC/Mod、全新账号完整设备授权（使用用户已有授权做过真实请求）。
